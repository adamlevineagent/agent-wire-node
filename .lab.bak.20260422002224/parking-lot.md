# Parking Lot

- Test with code corpus (vibesmithy) after finding a good docs framework — verify generalizability
- Test with mixed corpus (docs + code)
- Try having characterize.md drive the abstraction framework dynamically per-corpus
- Explore whether extraction_schema.md should be the lens-setting step rather than hardcoding in individual prompts
