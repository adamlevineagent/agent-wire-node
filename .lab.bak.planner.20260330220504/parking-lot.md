# Parking Lot — Planner Command Names

## Deferred Ideas
- Two-stage: classifier picks relevant vocabulary subset, planner gets only relevant commands
- Tool-use / function-calling mode instead of freeform JSON generation
- Fine-tune a small model on correct planner outputs
- XML-tagged vocabulary blocks instead of markdown headers
- Structured enum for command field in schema
