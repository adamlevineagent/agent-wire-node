# Parking Lot

- Compare current document clustering behavior against `core-selected-docstest8` if the simplified schema still yields 1:1 thread ratios.
- If code remains singleton-heavy after schema corrections, test the `concept_areas -> assign` architecture already present in prompts.
- If conversation grouping shows the same topic-assignment failure mode as code, mirror the document `source_node`-only schema as the first fix.
