# Code Pipeline Chain Definition — Semantic Grouping
#
# Pipeline:
#   1. L0 code extraction (per-file LLM analysis)
#   2. Thread clustering (LLM groups ALL L0 topics into 6-12 semantic threads)
#   3. Thread narrative synthesis (per-thread LLM synthesis into L1 nodes)
#   4. L1 cross-layer webbing
#   5. Upper layers (recursive adjacent pairing from L1 to apex)
#   6. L2 cross-layer webbing
#
# This produces meaningful semantic layers instead of blind 2:1 pairing.

schema_version: 1
id: code-default
name: Code Pyramid
description: >
  Code analysis pipeline with semantic grouping: per-file extraction,
  LLM-based thread clustering, per-thread narrative synthesis, then
  recursive pairing to apex.
content_type: code
version: "2.0.0"
author: agent-wire

defaults:
  model_tier: mid
  temperature: 0.3
  on_error: "retry(2)"

steps:
  # ── Step 1: L0 code extraction ────────────────────────────────────────
  # Per-file LLM analysis producing one node per source file.
  - name: l0_code_extract
    primitive: extract
    instruction: "$prompts/code/code_extract.md"
    for_each: $chunks
    concurrency: 8
    node_id_pattern: "C-L0-{index:03}"
    depth: 0
    save_as: node
    model_tier: mid
    temperature: 0.3
    on_error: "retry(3)"

  # ── Step 2: Thread clustering ─────────────────────────────────────────
  # Takes ALL L0 extraction topics and groups them into 6-12 semantic
  # threads. Uses qwen (1M context) because the topic inventory can be
  # very large for big codebases.
  # Input: the full array of L0 step outputs.
  # Output: JSON with "threads" array, each containing assignments.
  - name: thread_clustering
    primitive: classify
    instruction: "$prompts/code/code_cluster.md"
    input:
      topics: $l0_code_extract
    response_schema:
      type: object
      properties:
        threads:
          type: array
          minItems: 1
          items:
            type: object
            properties:
              name:
                type: string
                description: "2-6 word thread label"
              description:
                type: string
                description: "1-2 sentence description of the subsystem"
              assignments:
                type: array
                minItems: 1
                items:
                  type: object
                  properties:
                    source_node:
                      type: string
                      description: "Assigned L0 node ID copied EXACTLY from the input topic's node_id/source_node field (for example C-L0-005). Never put the headline text here."
                    topic_index:
                      type: integer
                      description: "Index of the topic within the topic inventory"
                    topic_name:
                      type: string
                      description: "Original headline or topic label"
                  required: ["source_node", "topic_index", "topic_name"]
                  additionalProperties: false
            required: ["name", "description", "assignments"]
            additionalProperties: false
      required: ["threads"]
      additionalProperties: false
    model: "qwen/qwen3.5-flash-02-23"
    temperature: 0.3
    on_error: "retry(3)"

  # ── Step 3: Thread narrative synthesis ─────────────────────────────────
  # For each thread from clustering, synthesize the assigned L0 topics
  # into a coherent narrative node.
  - name: thread_narrative
    primitive: synthesize
    instruction: "$prompts/code/code_thread.md"
    for_each: $thread_clustering.threads
    concurrency: 5
    # No response_schema — structured outputs constrain mercury-2 and cause
    # token clipping on large threads (49K+ tokens). Free-form JSON + retry
    # is more reliable and produces richer content.
    node_id_pattern: "L1-{index:03}"
    depth: 1
    save_as: node
    model_tier: mid
    temperature: 0.3
    on_error: "retry(2)"

  # ── Step 4: L1 cross-layer webbing ────────────────────────────────────
  # Identify cross-cutting relationships between sibling L1 nodes so the
  # UI can show "see also" edges across the tree.
  - name: l1_webbing
    primitive: web
    instruction: "$prompts/code/code_web.md"
    input:
      nodes: $thread_narrative
    response_schema:
      type: object
      properties:
        edges:
          type: array
          items:
            type: object
            properties:
              source:
                type: string
                description: "Exact sibling node ID from the input nodes array (for example L1-000). Never use the headline here."
              target:
                type: string
                description: "Exact sibling node ID from the input nodes array (for example L1-003). Never use the headline here."
              relationship:
                type: string
                description: "One concise sentence explaining the concrete connection"
              shared_resources:
                type: array
                items:
                  type: string
              strength:
                type: number
                description: "0.0-1.0 relevance score for the connection"
            required: ["source", "target", "relationship", "shared_resources", "strength"]
            additionalProperties: false
      required: ["edges"]
      additionalProperties: false
    depth: 1
    save_as: web_edges
    model_tier: mid
    temperature: 0.2
    on_error: "skip"

  # ── Step 5: Upper layers (recursive clustering L1 → apex) ────────────
  # Semantic re-clustering at each layer instead of blind 2:1 pairing.
  # Each layer: LLM groups nodes into 3-5 clusters → synthesize each cluster.
  # Produces 2-3 upper layers instead of 4-5, with better semantic structure.
  - name: upper_layer_synthesis
    primitive: synthesize
    instruction: "$prompts/code/code_distill.md"
    recursive_cluster: true
    cluster_instruction: "$prompts/code/code_recluster.md"
    cluster_model: "qwen/qwen3.5-flash-02-23"
    cluster_response_schema:
      type: object
      properties:
        clusters:
          type: array
          items:
            type: object
            properties:
              name:
                type: string
                description: "2-6 word cluster label — must be unique across all clusters"
              description:
                type: string
                description: "1-2 sentences on what this architectural area covers"
              node_ids:
                type: array
                items:
                  type: string
                description: "IDs of nodes in this cluster (e.g., L1-000, L1-003)"
            required: ["name", "description", "node_ids"]
            additionalProperties: false
      required: ["clusters"]
      additionalProperties: false
    depth: 1
    save_as: node
    node_id_pattern: "L{depth}-{index:03}"
    model_tier: mid
    temperature: 0.3
    on_error: "retry(3)"

  # ── Step 6: L2 cross-layer webbing ────────────────────────────────────
  # Runs after upper-layer synthesis completes and refreshes the peer edges
  # between architectural-domain nodes at depth 2.
  - name: l2_webbing
    primitive: web
    instruction: "$prompts/code/code_web.md"
    response_schema:
      type: object
      properties:
        edges:
          type: array
          items:
            type: object
            properties:
              source:
                type: string
                description: "Exact sibling node ID from depth 2 (for example L2-000). Never use the headline here."
              target:
                type: string
                description: "Exact sibling node ID from depth 2 (for example L2-001). Never use the headline here."
              relationship:
                type: string
              shared_resources:
                type: array
                items:
                  type: string
              strength:
                type: number
            required: ["source", "target", "relationship", "shared_resources", "strength"]
            additionalProperties: false
      required: ["edges"]
      additionalProperties: false
    depth: 2
    save_as: web_edges
    model_tier: mid
    temperature: 0.2
    on_error: "skip"

post_build: []
