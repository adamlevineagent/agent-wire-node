{
  "name": "wire-node-pyramid-mcp",
  "version": "0.2.0",
  "description": "MCP server for Wire Node pyramid queries",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "pyramid-cli": "dist/cli.js"
  },
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "test": "tsc -p tsconfig.test.json && node --test --test-reporter=spec dist-test/test/*.test.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.1"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "@types/node": "^22.0.0"
  }
}
